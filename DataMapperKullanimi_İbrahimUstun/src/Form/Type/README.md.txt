Veri eşleştiriciyi oluşturduktan sonra, kullanmak için formu yapılandırmanız gerekir. Bu, setDataMapper() yöntem kullanılarak elde edilir.
ColorTypeForm kullanırken, özel veri eşleyici yöntemleri yeni bir Color nesnesi oluşturacaktır.
